// import { repository } from "./repository";

// export const appConfigService = {
//   async getConfig({ server, db, config }: { server: string; db: string; config: string }) {
//     return repository.getConfig(server, db, config);
//   },
// };
